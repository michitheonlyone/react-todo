import {Button} from "react-bootstrap";


const AddTodo = () => {
    return (
        <form>
            {/*<Button variant="success" onClick={}>Add</Button>*/}
        </form>
    );
};

export default AddTodo;